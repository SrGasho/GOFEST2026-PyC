One-line: the M3 card — 12dp radius, one of three variants.

```jsx
<Card variant="outlined" interactive>…</Card>
```

Outlined for dense lists of records (cheapest visually), elevated when the card floats over a map, filled for a grouped settings block. Never nest a card in a card.
