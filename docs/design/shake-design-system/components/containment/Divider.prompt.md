One-line: a 1dp rule between related rows.

```jsx
<Divider inset={72}/>
<Divider label="Earlier today"/>
```

Prefer whitespace or a card boundary first; a divider is for rows that would otherwise run together. Inset 72 aligns past a 40dp avatar plus 16dp margins.
