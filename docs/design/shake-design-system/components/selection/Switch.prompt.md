One-line: an instant on/off setting — location sharing, alert channels.

```jsx
<Switch label="Share my location with responders" checked={on} onChange={setOn}/>
```

A switch applies immediately; if the change needs a Save button, use a Checkbox instead. The thumb grows 16→24dp on turning on — keep that motion.
