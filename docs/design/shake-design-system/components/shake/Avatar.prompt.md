One-line: the person identifier in every list, sheet and header.

```jsx
<Avatar name="Mei Tanaka" status="missing"/>
<Avatar icon="groups" size={56}/>
```

Most Shake records have no photo, so initials are the normal case, not a fallback. The status ring is redundant with a StatusChip in the same row — use one or the other.
