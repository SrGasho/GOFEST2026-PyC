One-line: a person’s status — the most important object in the product.

```jsx
<StatusChip status="safe"/>
<StatusChip status="missing" size="sm"/>
```

Never communicate status with colour alone: the glyph and the word are load-bearing for colour-blind users and for greyscale printouts pinned up at shelters. Do not invent new statuses — the five are the full lifecycle.
