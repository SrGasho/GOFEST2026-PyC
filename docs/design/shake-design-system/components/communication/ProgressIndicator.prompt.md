One-line: waiting states — syncing reports, uploading a photo.

```jsx
<ProgressIndicator/>
<ProgressIndicator variant="circular" size={40}/>
```

Linear sits directly under the top app bar, full bleed. Use determinate whenever an amount is actually known; a fake determinate bar is worse than an indeterminate one during an emergency.
