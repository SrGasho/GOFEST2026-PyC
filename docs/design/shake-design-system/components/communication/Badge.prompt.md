One-line: an unread count or a "something changed" dot.

```jsx
<Badge count={12}/>
<Badge dot color="tertiary"/>
```

Never use Badge to show a person’s status — that is StatusChip. Error red is the default because in Shake a badge always means unread alerts.
