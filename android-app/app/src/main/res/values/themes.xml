<resources>
    <!-- Splash/window background before Compose takes over; real theming lives in ui/theme (Compose). -->
    <style name="Theme.Shake" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
